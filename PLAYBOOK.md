# PLAYBOOK.md (retired)

Installer / casual-share distribution is **not** a supported Portskill path.

Use the developer quick start in **[README.md](README.md)** (`git clone` then `./scripts/run.sh`). Personal Mac packaging is keepalive + `build-app.sh` only. App Store Connect / notarization remain HOLD.
